- FastAPI basics, created REST API 
- Learned GET and POST endpoints, how routing works using @app decorators
- REST API concepts: GET (read), POST (create), PUT (update), DELETE (remove)
- Learned CRUD concepts

# Day 3 - FastAPI & Git Learning

## What I learned

### FastAPI basics
- Created REST API using FastAPI
- Understood GET and POST endpoints
- Learned how routing works using @app decorators
- Understood how JSON responses are returned automatically

### Pydantic models
- Used BaseModel to define request body structure
- Learned that models validate incoming data automatically
- Understood validation errors (422 Unprocessable Entity)

### REST API concepts
- GET → read data
- POST → create data
- PUT → update data
- DELETE → remove data
- Learned CRUD concept and how it maps to HTTP methods

### Git basics
- Used git add, commit, push
- Understood staging area vs commit vs push
- Learned branch concept (main vs feature branches)
- Understood remote origin and GitHub integration

### Issues encountered
- Git push failed due to branch tracking issues
- Learned about upstream branches (-u origin branch)
- Understood authentication using GitHub token

## Key takeaway
FastAPI simplifies backend development by combining routing, validation, and automatic documentation. Git requires understanding of branches and remote tracking for proper workflow.